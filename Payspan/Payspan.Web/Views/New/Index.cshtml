@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

@using(Ajax.BeginForm("Save", "New", new AjaxOptions { HttpMethod = "POST", InsertionMode = InsertionMode.Replace, UpdateTargetId = "Results" }))
{
      @Html.TextBox("Name");
      <input type="submit" value="Submit" />
}
<div id="Results">
</div>

@section scripts
{
    <script src="../../Scripts/jquery.unobtrusive-ajax.min.js" type="text/javascript"></script>
}